import { ProbabilityPipe } from './probability.pipe';

describe('ProbabilityPipe', () => {
  it('create an instance', () => {
    const pipe = new ProbabilityPipe();
    expect(pipe).toBeTruthy();
  });
});
